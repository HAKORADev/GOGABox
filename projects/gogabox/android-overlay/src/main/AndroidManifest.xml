<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:versionCode="1"
    android:versionName="1.0"
    android:installLocation="auto" >

    <!-- GOGABox overlay: the box rotates freely (fullSensor; games force their
         own orientation at runtime via GameHost) + network + Unity Ads plugin
         registration + local notification system (Notify plugin). -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- local notifications (timed game reveals + return reminders) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <supports-screens
        android:smallScreens="true"
        android:normalScreens="true"
        android:largeScreens="true"
        android:xlargeScreens="true" />

    <uses-feature
        android:glEsVersion="0x00030000"
        android:required="true" />

    <application
        android:label="@string/godot_project_name_string"
        android:allowBackup="false"
        android:icon="@mipmap/icon"
        android:appCategory="game"
        android:isGame="true"
        android:hasFragileUserData="false"
        android:requestLegacyExternalStorage="false"
        tools:ignore="GoogleAppIndexingWarning" >
        <profileable
            android:shell="true"
            android:enabled="true"
            tools:targetApi="29" />

        <!-- Unity Ads v1 plugin registration (value = GodotPlugin subclass) -->
        <meta-data
            android:name="org.godotengine.plugin.v1.UnityAds"
            android:value="org.godotengine.plugin.unityads.UnityAdsPlugin" />

        <!-- Local notification plugin registration -->
        <meta-data
            android:name="org.godotengine.plugin.v1.Notify"
            android:value="org.godotengine.plugin.notify.NotifyPlugin" />

        <activity
            android:name=".GodotApp"
            android:theme="@style/GodotAppSplashTheme"
            android:launchMode="singleInstancePerTask"
            android:excludeFromRecents="false"
            android:exported="false"
            android:supportsPictureInPicture="true"
            android:screenOrientation="fullSensor"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="layoutDirection|locale|orientation|keyboardHidden|screenSize|smallestScreenSize|density|keyboard|navigation|screenLayout|uiMode"
            android:resizeableActivity="false"
            tools:ignore="UnusedAttribute" />
        <activity-alias
            android:name=".GodotAppLauncher"
            android:targetActivity=".GodotApp"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <!-- Notify plugin: fires scheduled alarms + re-arms them after reboot -->
        <receiver
            android:name="org.godotengine.plugin.notify.NotifyPlugin$AlarmReceiver"
            android:exported="false" />
        <receiver
            android:name="org.godotengine.plugin.notify.NotifyPlugin$BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
