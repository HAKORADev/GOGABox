# PLAN v0.3.6-4 — GEOMETRY FLASH patch 3 (the renumbered patch; the shipped
# v0.3.6-3 was actually patch 2 — the owner's numbering)

## 1. THE COLLECT SIMPLIFICATION
- The orbit/coin collect wears JUST the yellow circle (one expanding ring).
  The golden glow pop + star flashes are gone (the owner: "simplify it to be
  just the yellow circle, same for gogacoins too").
- Probe: the collect adds ONE ring node and ZERO particles.

## 2. THE WORLD LAW 2
- THE SIDE PROFILES: the generator reads the square — on the roof the roof
  family triples and roof-only yards open; MIXED chunks always in the pool.
- THE BLOCK DECKS: the thin platforms are retired — long structured surfaces
  built FROM BLOCKS (plain runs, notched runs with a tower bump, twin decks
  on both line heights), every deck carries its own climb-up and step-down.
- THE CLIMB-DOWN: the dedicated chunk — 2-cell steps up to a 4-tall tower,
  then the real staircase down 3-2-1 to the floor.
- THE ROOF YARD: hanging block decks at the line heights + a marked lane +
  a floating spike row — the roof side has its own built world.
- THE MIXED PROFILE: one chunk filling ground stair + block deck + roof pads.
- The ladder rungs and the descent ride are BLOCK ROWS now (blocks are
  surfaces, not just the thin platforms).

## ship record
- [x] gf_probe 110 checks 0 fails (the collect-ring law + the deck/down/
      roof-yard/mixed/side-profile battery)
- [x] gf_soak CLEAN (5 seeds; seed 4 = a full 60s blind survival)
- [x] gf_replay_repro CLEAN
- [x] qa_v036p3 rigs: deck / down / mixed / roof_yard / collect_ring shot
      and eyeballed
- [x] version 0.3.6-4 / 30710 (arm32 30711 / arm64 30712) — NO release
