# MARBLE POPPER — PROGRESS (v040-13)

The owner's GDD laws -> implemented -> verified. One line per law.

## The owner's laws (all SHIPPED)

- [x] Rename to MARBLE POPPER, the zuma seat graduated (registry law)
- [x] Vertical design (portrait, the box design space 1080x1920)
- [x] Places NOT buyable - 10 places as difficulty tiers (colors 3->7, speed
      62->98, quota 34->52+, wave sizes shrink)
- [x] 100 levels, 10 per place (maps.json + maps_data.gd, generated by the
      forge; every place samples all 10 layout kinds)
- [x] 5 player skins + 5 marble skins + 5 hole skins, GOGACoins pricing
      (0/140/180/240/320), Box shop_items categories
- [x] LEVELS button after SHOP; places grid (5+5) then levels grid (5+5);
      map previews like Pop Siege (per-level rendered thumbs)
- [x] NORMAL / CHALLENGE on every unlocked level card
- [x] Challenge = 10 waves of the same level, each faster/longer/more colors
      (+12% speed, +25% quota per wave, +1 color per 3 waves), until lose
- [x] Score = +1 per level (+3 for a full challenge), bonus /5 (coin_div 5),
      no in-game currency
- [x] GOGACoin rides the chain every 10 waves from the last collected one,
      5s life, flickers then fades, missed coins re-appear in a later wave
- [x] Level variety: fixed shooters at 7 positions, slider (Luxor) levels,
      twin paths, twin switchable shooters with the fade-swap
- [x] Path preview before every level (arrows run the exact route, tap to skip)
- [x] Locked levels ladder until 100% complete; free jump after
- [x] Lives: 3, widget chip, loss ends the round (progress saved), +1 life
      every 10 levels, all lost = ladder resets to level 1, lives system
      removed at 100% (free play)
- [x] Level cleared card: NEXT LEVEL / NEXT PLACE / REPLAY / LEVELS
- [x] Marbles slow before the hole (last 15% at 0.55x), danger pulse + beat
- [x] Level length hidden (quota), spawning stops when reached
- [x] Connection law: gap closes, matching ends join and cascade, non-matching
      ends wait and move together (the bond law, probe-verified)
- [x] 6 powers bought from the shop so they spawn: back / bomb / speed /
      vapor / rainbow / lightning; glowing marbles in the chain; name text
      with black outline on pickup; unmatched pow marbles vanish in 10s
- [x] Pow spawn rate ~1-2/60s (first 20s, then 26-40s), owned kinds only
- [x] No powerup timer widget (the owner's no-reveal law)
- [x] Thumbnail from the real game assets (composed scene, 960x640)
- [x] Guide (registry desc + 8 how-to lines), 6 achievements (tiers 1-4),
      entry (fee 5, price 350), reveal continuity from the teaser
- [x] Audio: 33 mb_* SFX + 5 themes cut + code-modified from the studies
- [x] Real emulated tests: 34-check chain-law probe ALL GREEN + the full
      flow_test ALL PASSED + the 16-still Xvfb film (eye-passed twice)

## The bugs the films caught (fixed the same round)

1. `holes` array never cleared between levels (freed refs on the next level)
2. empty-chain crash in the movement pass after a full clear
3. the chain array order law (waves appended at the tail vs d-ascending)
4. the join flag died before the bite (gap_open -> the bond law hysteresis)
5. shop skin rows showed prices without names
6. twin levels had no visible pad at the idle swap spot
7. places-card label overflow at the sheet edge
8. the film rig itself: story gate bypass, wrong level indices

## Studies (the owner's scrape order)

- `study_locker/gamesnacks/totemia/` - full site + 90 levels extracted
- `study_locker/gamesnacks/zumble_ocean/` - full site + 60 levels extracted
- the atlases drove the marble/launcher/idol art (cut + code-modified),
  the sounds drove the mb_* audio set
