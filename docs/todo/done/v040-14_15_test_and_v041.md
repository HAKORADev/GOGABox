# TEST v040-15 (+ the full v040-14 round) — DELIVERED in the v041 round

The owner tested the PC build and the v040-14/15 games together (the mixed
report, 2026-09-20) and v041 worked every item:

- **Gold Miner (v040-15 report)**: the intro ghost miner kill (the rig, rope
  and claw seat in only when the run starts; the intro rides the universal
  tap-anywhere overlay), the rig stands EXACTLY on the measured surface line,
  the claw's whole trip clamps to the visible room (nothing reels from
  out-of-resolution ground), rocks pay NEGATIVE (-2/-4/-6, the owner's
  override of the old GDD) with the float-text tax at the winch, and the
  grounds grew real traps (every big gold's aim lane rides a guard; the
  fairness validator stays). The thumbnail is a code-programmed in-game
  capture (the rig ON the surface, the field below).
- **Marble Popper (v040-14 report + the v041 additions)**: the universal
  tap-anywhere intro, the PC mouse seat (cursor aims, LMB fires, RMB swaps),
  the sparkle route preview (never ghost marbles), the true jaw/hole/head
  layering, THE POP-BACK LAW (the part toward the hole rolls BACK, faster,
  colors as-is), the true rolling read (the face tumbles over the top on
  horizontal lanes, no self-spin), THE LIVING INSERT (the shot sneaks in
  while the forward part is pushed one spacing over time), per-marble pop
  colors, the powerup carrier as a fixed-color normal marble (icon only,
  match-popped, never any-shot), the VAPOR sweep (the chain dissolves
  marble by marble, matcher-style), the extinct-color oracle (the shooter
  never loads a dead color), and the dev-sheet reopen verified clean on a
  dedicated probe (the owner's v040-14/15 crash seat - if it ever restarts
  again on v041, the report pins it).
- **Rock Breaker** chaos bounces and **Deadly Worm** stay-open laws shipped
  in v040-14 unchanged (no new report items arrived for them).

Version note: the fix round was versioned **v041** by the owner's order
("make this as v041 and not a v040 patch"), config 0.4.1 / code base 31300.
